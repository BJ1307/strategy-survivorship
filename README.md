# strategy-survivorship

Research on trading-strategy **survivorship**: survivorship bias in backtests,
strategy decay, and out-of-sample persistence.

## Status

Scaffold only — nothing implemented yet.

## Layout (planned)

```
data/       raw + derived datasets (gitignored)
src/        library code
notebooks/  exploratory analysis
tests/      unit tests
```

## Setup

```bash
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt   # once it exists
```
